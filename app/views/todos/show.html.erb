<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        todo詳細
      </h1>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-7">
        <div class="card">
          <div class="card-content">
            <div class="media">
              <div class="media-content">
                <p class="title is-4"><%= @todo.task %></p>
              </div>
            </div>
            <div class="content">
              <table class="table is-narrow">
                <tr>
                  <th>詳細</th>
                </tr>
                <tr>
                  <td><%= simple_format @todo.detail %></td>
                </tr>
              </table>
              <% if current_user.already_favorited?(@todo) %>
                <%= link_to todo_favorites_path(@todo), method: :delete do %>
                  <i class="fas fa-heart"></i>
                <% end %>
              <% else %>
                <%= link_to todo_favorites_path(@todo), method: :create do %>
                  <i class="far fa-heart"></i>
                <% end %>
              <% end %>
              <%= @todo.favorites.count %>
              <% if @todo.user.id == current_user.id %>
                <%= link_to "編集画面へ", edit_todo_path(@todo), class: "button is-success" %>
              <% end %>
            </div>
          </div>
        </div>
      </div>

      <div class="column is-4">
        <article class="panel is-link">
          <p class="panel-heading">
            By <%= @todo.user.username %>
          </p>
          <div class="panel-block">
            <p class="control">
              <%= @todo.user.profile %>
            </p>
          </div>
          <%= link_to user_path(@todo.user), class: "panel-block" do %>
            <span class="panel-icon">
              <i class="fas fa-user" aria-hidden="true"></i>
            </span>
            <%= @todo.user.username %> さんのページへ
          <% end %>
        </article>
      </div>
    </div>
  </div>
</section>
